pre-commit:
  commands:
    secret-scan:
      tags: security
      run: talisman --githook pre-commit
    lint:
      run: pnpm lint:fix
    format-check:
      run: pnpm format:fix

pre-push:
  parallel: true
  commands:
    unit-tests:
      run: pnpm test
      only:
        - ref: main
    typecheck:
      run: pnpm typecheck
      only:
        - ref: main
